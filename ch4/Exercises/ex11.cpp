#include <iostream>
#include <vector>

std::vector<int> findPrimes(int max) {
   std::vector<int> primes;
 
   return primes;
}

int main() {
   for (const auto& e : findPrimes(100)) {
      std::cout << e << '\n';
   }
   return 0;
}
