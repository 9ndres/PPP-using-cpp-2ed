#include <iostream>
#include <string>
#include <array>
// This solution could been easier if std::map were used.
int main(){
    constexpr quantity = 5;
    std::array<std::string, quantity> coinNames { "pennies", "nickels", "dimes", "quarters", "half dollars" };
    
}
